# ==========================================
# Simple Web Server for Local Hosting
# This script starts a simple HTTP server on your local machine, 
# useful for local testing or serving content.
# It uses Python's built-in HTTP server functionality.
# ==========================================

# Start a local HTTP server on port 8000 (requires Python installed)
python -m http.server 8000
